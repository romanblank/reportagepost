@AGENTS.md

# CLAUDE.md — Reportage Post

**Reportage Post (reportagepost.com)** — платформа-сообщество репортажных/событийных фотографов по модели mywed.com: каталог + портфолио + рейтинг + заявки + PRO-подписка. РФ-старт, архитектурный задел на мир.

## Если ты новая сессия — прочитай в этом порядке
1. `VISION.md` (корень репо) — что строим и зачем, метрика №1.
2. `../docs-vault/Проект/GLOBAL-PLAN.md` — живой план; работа = следующий незакрытый пункт ближайшей фазы. «Что дальше?» не существует.
3. Последний state-файл в памяти проекта (`~/.claude/projects/-Users-Blank-Documents-----------myrep/memory/`, секция «🔥 ПОДХВАТ СЕССИИ» в MEMORY.md).

## Текущее состояние (2026-07-13)
- **Сделано**: аудит MyWed (vault/audits), VISION и GLOBAL-PLAN подтверждены, дисциплина и память развёрнуты, стек одобрен (Next.js+TS+Prisma+PG+Tailwind), каркас репо создан.
- **Следующее блокирующее**: доступы к инфраструктуре от оператора (DNS, VM, SMS, почта) → чек-лист S0 в GLOBAL-PLAN.

## Ключевые инварианты продукта
- До публичного запуска (S4) платформа ЗАКРЫТА: noindex + robots.txt disallow + инвайт-доступ. Снятие — только явным пунктом плана S4.
- Планка качества — MyWed: первое впечатление у искушённой аудитории единственное.
- Глобальный задел с первого дня: i18n-ключи (никаких зашитых строк UI), URL с /ru/, гео-дерево страна→регион→город, деньги в минорных единицах + валюта, UTC в базе, телефоны E.164, платёжный слой и хранилище за абстракциями.
- Секреты — только в .env (вне git) и в vault/credentials.md (вне репо). Никогда в код, логи, коммиты.

## Архитектура и запуск локально
- Next.js 16 (App Router, src/, сборка ТОЛЬКО webpack), TypeScript strict, Tailwind CSS 4, Prisma 7 + PostgreSQL, ESLint. Тесты: Vitest (юниты/смоук), Playwright (e2e, добавится в S2).
- Запуск: `docker compose up -d` (PG на 5434; 5433 занят другой платформой оператора) → `npx prisma generate` → `npm run dev`. Миграции: `npx prisma migrate dev`.
- Prisma 7: URL БД в `prisma.config.ts` (не в schema), рантайм через `@prisma/adapter-pg` (`src/lib/db.ts`).
- `npm run build` — прод-сборка; `npm test` — тесты.
- Деплой: Docker-образ на VM Yandex Cloud за nginx (цепочка появится в S0-инфраструктуре).

## Verification Loop
После ЛЮБОГО изменения кода: `npm test` + `npm run build`. Эталон фиксируется здесь и обновляется при легитимном росте. Новые падения недопустимы. UI-изменения — проверять визуально.
- **Эталон тестов**: 21 passed (smoke, db.schema, geo-data×3, category-data×2, auth×3, photos×4, moderation×2, catalog×2, inquiries×3).
- Урок live-проверок HTML: React/SSR вставляет `<!-- -->` между JSX-интерполяциями — grep по составному тексту ложно падает; проверять по href/атрибутам. db.schema требует локальный PG (env-зависимость, правило c): без DATABASE_URL — skip, это не падение.

## Уроки CI/деплоя (2026-07-13, первая настройка пайплайна)
- **Лок-файл и платформенные optional deps** (sharp/emnapi): `npm install` на macOS пишет неполный lock → `npm ci` в Linux-CI падает. Лечение только полной пересборкой: `rm -rf node_modules package-lock.json && npm install`. `--package-lock-only` НЕ помогает.
- **Тесты самодостаточны**: интеграционный тест сам создаёт нужные ему записи БД (и убирает за собой). Полагаться на «в моей базе уже есть фотограф» = зелёное локально, красное в CI.
- **Dockerfile**: `ENV NODE_ENV=production` ставить ПОСЛЕ `npm ci` и `npm run build` — иначе npm пропустит dev-зависимости (tailwind/ts), нужные билду.
- **zsh**: `$USERNAME` — зарезервированная переменная (= имя OS-пользователя), не использовать в скриптах проверок.
- **tsx-скрипты**: обязательный `import 'dotenv/config'`; инлайн `tsx -e` с `$` в bash ломается на экранировании — писать временный файл В ПРОЕКТЕ (module resolution от расположения файла).

## Сетевая специфика оператора
- С ноутбука оператора провайдер режет путь к диапазонам Yandex Cloud (SSH/HTTP к VM недоступны, снаружи VM жива). Все операции с VM — ТОЛЬКО через GitHub Actions (ssh с раннеров). Проверка гипотезы при смене сети: `nc -vz <VM_IP> 22`.

## Правило падающих тестов (a/b/c)
(a) тест устарел относительно новой правильной логики → обновить тест;
(b) регрессия → чинить код, не тест;
(c) env-зависимость → задокументировать здесь и в тесте.
«Baseline drift» как отговорка запрещён.

## Code Conventions
- TypeScript strict, без `any` (см. `.claude/rules/typescript.md`).
- Строки UI — только через i18n-словарь (`src/i18n/`), в компонентах зашитых строк нет.

## Things Claude Should NOT Do
- Не включать индексацию/снимать noindex вне пункта S4 плана.
- Не коммитить без прохождения scripts/prepare-deploy.sh.
- Не синкать на сервер без --exclude='.env*'.
- Не затирать этот файл шаблонами скаффолдеров (уже случилось 2026-07-13 — восстановлен; урок: после генераторов проверять корневые файлы).
- НЕ прогонять prepare-deploy.sh через пайп (`| tail`) в связке с `&&` — пайп маскирует exit-код, и коммит проходит при красном gate (случилось 2026-07-13). Правильно: gate отдельной командой, коммит следующей. И не запускать gate, пока жив dev-сервер (билд конфликтует с .next).
- Не использовать Turbopack (env-зависимость, правило c): падает на кириллице в пути проекта («…/Платформы/…», TurbopackInternalError char boundary, Next 16.2.10). dev и build — через `--webpack`. Пересмотреть при переезде репо на ASCII-путь или фиксе бага.

---
_Любая ошибка Claude → обновление этого файла. Каждая ошибка = урок._
